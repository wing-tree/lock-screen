package com.flow.android.kotlin.lockscreen.notification.adapter

import androidx.viewbinding.ViewBinding
import com.flow.android.kotlin.lockscreen.databinding.NotificationBigTextBinding
import com.flow.android.kotlin.lockscreen.notification.model.NotificationModel

class NotificationViewBinder {
    fun bindBigText(binding: NotificationBigTextBinding, notification: NotificationModel) {

    }
}